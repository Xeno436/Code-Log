#include <iostream>
#include <algorithm>
#include <cstring>
using namespace std;
int const maxn = 1e5 + 7;
int t, n, m, k;
int bla[maxn];
string s;
void solve(int t)
{
    scanf("%d%d", &n, &m);
    cin >> s;
}
int main()
{
    scanf("%d", &t);
    while (t--)
    {
        solve(t);
        if (t)
            printf("\n");
    }
    return 0;
}