#include <iostream>
using namespace std;
int const maxn = 1e5 + 7;
int n;
int idx[maxn];
int ans[maxn];
int main()
{
    scanf("%d", &n);
    for (int i = 1; i <= n; ++i)
    {
        idx[i] = i;
        ans[i] = 1;
    }
    for (int i = 1; i <= n; ++i)
    {
        }
}